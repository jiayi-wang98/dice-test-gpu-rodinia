// DICECompiler Metadata for: stencil3d
// RF-unified v2: 10 DBBs. STS + dedicated bar+branch DBB dropped.
FUNCTION = _Z16stencil3d_kernelPKiPiiii;
SRF_PUBLICATION_REGS = (%r10);

DBB_ID = 0,
BITSTREAM_ADDR = $DICE_BB0_0,
BITSTREAM_LENGTH = 5,
UNROLLING_FACTOR = 1,
UNROLLING_STRATEGY = 0,
LAT = 1,
LD_DEST_REGS = (%c0, %c1, %c2, %c3, %c4),
IS_PARAMETER_LOAD;

DBB_ID = 1,
BITSTREAM_ADDR = $DICE_BB0_1,
BITSTREAM_LENGTH = 23,
UNROLLING_FACTOR = 1,
UNROLLING_STRATEGY = 0,
LAT = 9,
IN_REGS = (%c0, %c2, %c3, %c4),
OUT_REGS = (%p1, %r0, %r1, %r2, %r3, %r4, %r5, %r7, %r8, %r9),
LD_DEST_REGS = (%r10),
BRANCH = 1,
BRANCH_PRED = (%p1),
BRANCH_TARGET = 9,
BRANCH_RECVPC = 9;

DBB_ID = 2,
BITSTREAM_ADDR = $DICE_BB0_2,
BITSTREAM_LENGTH = 13,
UNROLLING_FACTOR = 1,
UNROLLING_STRATEGY = 0,
LAT = 5,
IN_REGS = (%r0, %r2, %r4),
OUT_REGS = (%p3, %p4, %r11),
LD_SRF_REGS = (%r0, %r12),
DISPATCH_II = 1,
BARRIER;

DBB_ID = 3,
BITSTREAM_ADDR = $DICE_BB0_3,
BITSTREAM_LENGTH = 7,
UNROLLING_FACTOR = 2,
UNROLLING_STRATEGY = 0,
LAT = 5,
IN_REGS = (%c2, %r1, %r9, %p3, %p4),
OUT_REGS = (%p7, %p8),
LD_DEST_REGS = (%r15, %r16);

DBB_ID = 4,
BITSTREAM_ADDR = $DICE_BB0_4,
BITSTREAM_LENGTH = 8,
UNROLLING_FACTOR = 2,
UNROLLING_STRATEGY = 0,
LAT = 4,
IN_REGS = (%r2, %r11),
OUT_REGS = (%p3, %p4),
LD_SRF_REGS = (%r2, %r13),
DISPATCH_II = 1;

DBB_ID = 5,
BITSTREAM_ADDR = $DICE_BB0_5,
BITSTREAM_LENGTH = 11,
UNROLLING_FACTOR = 2,
UNROLLING_STRATEGY = 0,
LAT = 6,
IN_REGS = (%c2, %c3, %r3, %r9, %p3, %p4),
OUT_REGS = (%p9, %p10),
LD_DEST_REGS = (%r17, %r18);

DBB_ID = 6,
BITSTREAM_ADDR = $DICE_BB0_6,
BITSTREAM_LENGTH = 8,
UNROLLING_FACTOR = 2,
UNROLLING_STRATEGY = 0,
LAT = 4,
IN_REGS = (%r4, %r11),
OUT_REGS = (%p3, %p4),
LD_SRF_REGS = (%r4, %r14),
DISPATCH_II = 2;

DBB_ID = 7,
BITSTREAM_ADDR = $DICE_BB0_7,
BITSTREAM_LENGTH = 12,
UNROLLING_FACTOR = 2,
UNROLLING_STRATEGY = 0,
LAT = 7,
IN_REGS = (%c2, %c3, %c4, %r5, %r9, %p3, %p4),
OUT_REGS = (%p11, %p12),
LD_DEST_REGS = (%r19, %r20);

DBB_ID = 8,
BITSTREAM_ADDR = $DICE_BB0_8,
BITSTREAM_LENGTH = 15,
UNROLLING_FACTOR = 1,
UNROLLING_STRATEGY = 0,
LAT = 8,
IN_REGS = (%c1, %r0, %r2, %r4, %r8, %r10, %r12, %r13, %r14, %r15, %r16, %r17, %r18, %r19, %r20, %p7, %p8, %p9, %p10, %p11, %p12),
STORE = 1;

DBB_ID = 9,
BITSTREAM_ADDR = $DICE_BB0_9,
RET;
