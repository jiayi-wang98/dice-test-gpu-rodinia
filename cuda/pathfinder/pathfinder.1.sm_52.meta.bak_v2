// DICECompiler Metadata for: pathfinder (RF-unified, conservative)
FUNCTION = _Z14dynproc_kerneliPiS_S_iiii;

DBB_ID = 0,
BITSTREAM_ADDR = $DICE_BB0_0,
BITSTREAM_LENGTH = 7,
UNROLLING_FACTOR = 1,
UNROLLING_STRATEGY = 0,
LD_DEST_REGS = (%c0, %c1, %c2, %c3, %c4, %c5, %c6),
IS_PARAMETER_LOAD;

DBB_ID = 1,
BITSTREAM_ADDR = $DICE_BB0_1,
BITSTREAM_LENGTH = 19,
UNROLLING_FACTOR = 1,
UNROLLING_STRATEGY = 0,
LAT = 7,
IN_REGS = (%c0, %c2, %c4, %c6),
OUT_REGS = (%p2, %r0, %r1, %r2, %r3, %r4, %r24),
LD_DEST_REGS = (%r7);

DBB_ID = 4,
BITSTREAM_ADDR = $DICE_BB0_4,
BITSTREAM_LENGTH = 9,
UNROLLING_FACTOR = 2,
UNROLLING_STRATEGY = 0,
LAT = 4,
IN_REGS = (%c0, %c4, %r1, %r24),
OUT_REGS = (%p3, %r25),
BRANCH = 1,
BRANCH_PRED = (%p3),
BRANCH_TARGET = 17,
BRANCH_RECVPC = 17;

DBB_ID = 5,
BITSTREAM_ADDR = $DICE_BB0_5,
BITSTREAM_LENGTH = 16,
UNROLLING_FACTOR = 1,
UNROLLING_STRATEGY = 0,
LAT = 4,
IN_REGS = (%c0, %c4, %c5, %c6, %r0, %r2, %r24, %r25),
OUT_REGS = (%p6, %r4, %r8, %r9, %r10, %r11);

DBB_ID = 6,
BITSTREAM_ADDR = $DICE_BB0_6,
BITSTREAM_LENGTH = 14,
UNROLLING_FACTOR = 2,
UNROLLING_STRATEGY = 0,
LAT = 4,
IN_REGS = (%c1, %p6, %r2, %r4, %r8, %r9, %r11),
OUT_REGS = (%p11, %r0, %r1, %r5),
LD_DEST_REGS = (%r20),
DISPATCH_II = 1,
BARRIER;

DBB_ID = 9,
BITSTREAM_ADDR = $DICE_BB0_9,
BITSTREAM_LENGTH = 13,
UNROLLING_FACTOR = 1,
UNROLLING_STRATEGY = 0,
LAT = 3,
IN_REGS = (%c0, %c4, %p11, %r5, %r7, %r10, %r11, %r20, %r23, %r25, %r4),
OUT_REGS = (%r5, %r7, %r10, %r11, %r31, %r4),
BRANCH = 1,
BRANCH_PRED = (%p30),
BRANCH_TARGET = 6,
BRANCH_RECVPC = 14;

DBB_ID = 14,
BITSTREAM_ADDR = $DICE_BB0_14,
BITSTREAM_LENGTH = 4,
UNROLLING_FACTOR = 4,
UNROLLING_STRATEGY = 0,
LAT = 2,
IN_REGS = (%r5, %c3, %r3, %r31),
STORE = 1;

DBB_ID = 17,
BITSTREAM_ADDR = $DICE_BB0_17,
RET;
