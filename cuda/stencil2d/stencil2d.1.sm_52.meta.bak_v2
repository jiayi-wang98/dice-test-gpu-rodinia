// DICECompiler Metadata for: stencil2d
// RF-unified v2: 8 DBBs.  STS + dedicated bar+branch DBB dropped.
FUNCTION = _Z16stencil2d_kernelPKiPiii;
SRF_PUBLICATION_REGS = (%r7);

DBB_ID = 0,
BITSTREAM_ADDR = $DICE_BB0_0,
BITSTREAM_LENGTH = 4,
UNROLLING_FACTOR = 1,
UNROLLING_STRATEGY = 0,
LAT = 1,
LD_DEST_REGS = (%c0, %c1, %c2, %c3),
IS_PARAMETER_LOAD;

DBB_ID = 1,
BITSTREAM_ADDR = $DICE_BB0_1,
BITSTREAM_LENGTH = 17,
UNROLLING_FACTOR = 2,
UNROLLING_STRATEGY = 0,
LAT = 8,
IN_REGS = (%c0, %c2, %c3),
OUT_REGS = (%p0, %p1, %p2, %r0, %r1, %r2, %r3, %r4, %r5, %r6),
LD_DEST_REGS = (%r7),
BRANCH = 1,
BRANCH_PRED = (%p2),
BRANCH_TARGET = 7,
BRANCH_RECVPC = 7;

DBB_ID = 2,
BITSTREAM_ADDR = $DICE_BB0_2,
BITSTREAM_LENGTH = 11,
UNROLLING_FACTOR = 2,
UNROLLING_STRATEGY = 0,
LAT = 5,
IN_REGS = (%r0, %r2),
OUT_REGS = (%p0, %p1, %r8),
LD_SRF_REGS = (%r9, %r2),
DISPATCH_II = 2,
BARRIER;

DBB_ID = 3,
BITSTREAM_ADDR = $DICE_BB0_3,
BITSTREAM_LENGTH = 11,
UNROLLING_FACTOR = 2,
UNROLLING_STRATEGY = 0,
LAT = 6,
IN_REGS = (%c2, %c3, %r3, %r6, %p0, %p1),
OUT_REGS = (%p5, %p6),
LD_DEST_REGS = (%r10, %r11);

DBB_ID = 4,
BITSTREAM_ADDR = $DICE_BB0_4,
BITSTREAM_LENGTH = 8,
UNROLLING_FACTOR = 2,
UNROLLING_STRATEGY = 0,
LAT = 4,
IN_REGS = (%r0, %r8),
OUT_REGS = (%p3, %p4),
LD_SRF_REGS = (%r3, %r0),
DISPATCH_II = 1;

DBB_ID = 5,
BITSTREAM_ADDR = $DICE_BB0_5,
BITSTREAM_LENGTH = 7,
UNROLLING_FACTOR = 2,
UNROLLING_STRATEGY = 0,
LAT = 5,
IN_REGS = (%c2, %r1, %r6, %p3, %p4),
OUT_REGS = (%p7, %p8),
LD_DEST_REGS = (%r12, %r13);

DBB_ID = 6,
BITSTREAM_ADDR = $DICE_BB0_6,
BITSTREAM_LENGTH = 12,
UNROLLING_FACTOR = 2,
UNROLLING_STRATEGY = 0,
LAT = 6,
IN_REGS = (%c1, %r0, %r2, %r3, %r5, %r7, %r9, %r10, %r11, %r12, %r13, %p5, %p6, %p7, %p8),
STORE = 1;

DBB_ID = 7,
BITSTREAM_ADDR = $DICE_BB0_7,
RET;
